package practice.DS;

public class BinaryTreeNode {
    public int data;
    public BinaryTreeNode left;
    public BinaryTreeNode right;

    public BinaryTreeNode(int data) {
        this.data = data;
    }
/* public int maxInBinaryTree(BinaryTreeNode root){
        int dataMax= Math.max(root.right.data, root.left.data);
        Heapsort()
        if (dataMax > root.data) {
            return dataMax;
        }else{
            return root.data;
        }

        if(root.left !=null) {
            maxInBinaryTree(root.left);
        }
        if ()
        maxInBinaryTree()
    }*/
}
